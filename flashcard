#!/usr/bin/env sh

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
JAR_FILE="$SCRIPT_DIR/target/flashcard-1.0.0-SNAPSHOT.jar"

if [ ! -f "$JAR_FILE" ]; then
  echo "Build the project first with: mvn package" >&2
  exit 1
fi

exec java -jar "$JAR_FILE" "$@"
